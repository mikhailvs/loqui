# secrets — never commit the TLS keypair
.cert.pem
.key.pem
# large model weights
models/
*.gguf
# generated audio + transient
audio/
.voicetmp/
# learner state (per-run) + passkey progress store
learner_state.json
.learner_*.json
progress/
# python
__pycache__/
*.pyc
.venv/
